apiVersion: v1
kind: Service
metadata:
  name: web-app
  namespace: web-app
spec:
  type: LoadBalancer
  loadBalancerIP: 195.154.196.22
  selector:
    app.kubernetes.io/name: web-app
  ports:
    - name: http
      port: 80
      targetPort: http
